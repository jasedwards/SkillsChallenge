import { CustomLoadingPipe } from './custom-loading.pipe';

describe('CustomLoadingPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomLoadingPipe();
    expect(pipe).toBeTruthy();
  });
});
